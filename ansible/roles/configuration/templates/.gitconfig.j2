[core]
	excludesfile = /Users/shinji/.gitignore_global
  editor = vim -c \"set fenc=utf-8\"
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = -
[mergetool "sourcetree"]
	cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
[user]
	name = shinji
	email =
[commit]
	template = /Users/shinji/.stCommitMsg


[alias]
  lg1 = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
  lg2 = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
  lg = !"git lg1"

{% if http_proxy != "" %}
[http]
  proxy = {{ http_proxy }}
{% endif %}
{% if https_proxy %}
[https]
  proxy = {{ https_proxy }}
{% endif %}
[credential]
  helper = cache --timeout=9600

# go get from private repo
# https://gist.github.com/shurcooL/6927554
# when execute command: `git config --global credential.helper store`
#[credential]
#	helper = cache --timeout=9600


#```
#Credentials may be stored in the ~/.git-credentials file.
#
#git config --global credential.helper store
#echo "https://${GITHUB_TOKEN}:x-oauth-basic@github.com" >> ~/.git-credentials
#git:// protocol is less secure
#
#git config --global url."https://github.com".insteadOf git://github.com
#```
