PATH="$HOME/bin:$HOME/.local/bin:$PATH"

export http_proxy="{{ http_proxy }}"
export https_proxy="{{ https_proxy }}"
export ftp_proxy=$http_proxy
export HTTP_PROXY=$http_proxy
export HTTPS_PROXY=$https_proxy
export rsync_proxy=$http_proxy
export no_proxy=127.0.0.1, localhost

if [ -f $HOME/.proxy.sh ]; then
  echo "test"
  $HOME/.proxy.sh
fi

if [ -d $HOME/.anyenv ]; then
    export PATH="$HOME/.anyenv/bin:$PATH"
    eval "$(anyenv init -)"
    # for tmux
  #  for D in `ls $HOME/.anyenv/envs`
  #  do
  #    export PATH="$HOME/.anyenv/envs/$D/bin:$PATH"
  #  done
fi

export GOPATH="$HOME/go"
PATH="$GOPATH/bin:$PATH"
PATH="/usr/local/bin/go/bin:$PATH"

eval "$(direnv hook bash)"


# add nifty cloud cli
NIFTY_CLOUD_HOME=/usr/local/NIFTY_Cloud_api-tools
export NIFTY_CLOUD_HOME
PATH="$PATH:$NIFTY_CLOUD_HOME/bin"

# java
JAVA_HOME="/usr/local/lib/jdk1.8.0_144"
PATH="$PATH:$JAVA_HOME/bin"
export JAVA_HOME

export PATH

